{% extends 'base.html.twig' %}

{% block title %}Gruppen für {{ video.title }} verwalten{% endblock %}

{% block body %}
    <h1>Gruppen für {{ video.title }} verwalten</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Speichern</button>
    {{ form_end(form) }}
{% endblock %}
